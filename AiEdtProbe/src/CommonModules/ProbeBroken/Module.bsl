// Модуль СЛОМАН НАМЕРЕННО. Не чинить.
//
// Это единственный в стенде источник настоящей ошибки компиляции. На нем проверяется, что разбор
// проблем проекта действительно что-то находит: на исправном проекте такая проверка проходит и
// когда инструмент сломан, потому что "ошибок нет" и "ошибки не ищутся" выглядят одинаково.
//
// Ошибка ниже - незакрытая процедура: нет КонецПроцедуры.

#Region Public

// Никогда не компилируется: у процедуры намеренно отсутствует завершение.
//
Процедура ЗдесьНетЗавершения() Экспорт

	ВызватьИсключение "Этот код никогда не компилируется";
