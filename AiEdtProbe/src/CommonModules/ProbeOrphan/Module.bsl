// Экспортный метод, который никто не вызывает.
//
// Нужен как заведомо мертвый код: на нем проверяется поиск неиспользуемых процедур. Если этот
// метод где-то понадобится - не вызывать его, а завести отдельный; иначе проверка потеряет свой
// единственный положительный случай.

#Region Public

// Ничего не делает и никем не вызывается.
//
// Параметры:
//  Reason - Строка - для чего вызвали; не используется.
//
Procedure NeverCalled(Reason) Export

	// Тело намеренно пустое: важен сам факт недостижимости, а не содержимое.

EndProcedure

#EndRegion
